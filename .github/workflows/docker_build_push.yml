name: ci container build-push

on:
  push:
    tags:
      - "v*.*.*"

env:
  LATEST_TAG: ghcr.io/collaborativebioinformatics/bifrost:latest

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          # list of Docker images to use as base name for tags
          images: |
            ghcr.io/${{ github.repository_owner }}/bifrost       
          tags: |
            type=schedule
            type=ref,event=branch
            type=ref,event=pr
            type=pep440,pattern={{version}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha
            type=raw,value=latest,enable={{is_default_branch}} 
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}      

      - name: Build and export to Docker
        uses: docker/build-push-action@v6
        with:
          context: "{{defaultContext}}:."
          load: true
          tags: ${{ env.LATEST_TAG }}
          file:
            ./Dockerfile

      - name: Run unit tests
        run: |
          docker run --rm -v ${{ github.workspace }}:/home ${{ env.LATEST_TAG }} python -m pytest -q -m "not slow"
            
      - name: Scan image
        id: anchor_scan
        uses: anchore/scan-action@v7
        with:
          image: ${{ env.LATEST_TAG }}
          fail-build: false
          output-format: table
          severity-cutoff: critical

      # - name: Inspect scan action table report
      #   run: cat ${{ steps.anchor_scan.outputs.sarif }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: "{{defaultContext}}:."
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          file:
            ./Dockerfile
          platforms: linux/amd64
